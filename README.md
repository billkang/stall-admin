# Stall Admin

Stall-Admin 是一个基于 Vue 3、Vite、TypeScript 和 Turborepo 构建的现代中后台管理系统解决方案。它不仅提供了一个开箱即用的前端模版，还包含完善的文档系统和后端 Mock 服务，适合用于企业级中后台项目开发或作为学习参考。

## ✨ 特性

- **最新技术栈**：采用 Vue 3, Vite, TypeScript, Tailwind CSS 等前沿技术。
- **Monorepo 架构**：基于 Turborepo 的高性能单仓库管理，代码共享更便捷。
- **类型安全**：全量 TypeScript 编写，提供完整的类型定义。
- **组件库**：集成 Ant Design Vue (及其他适配器)，支持灵活的组件扩展。
- **主题系统**：强大的主题配置，内置多套配色方案，支持暗黑模式。
- **国际化**：内置完善的 i18n 国际化方案。
- **权限管理**：精细的路由级别权限控制。
- **Mock 服务**：内置 Nitro 驱动的高性能 Mock 服务。
- **文档完善**：基于 VitePress 的详细开发文档。

## 📂 项目结构

```
stall-admin/
├── apps/
│   ├── web/            # 主应用 (Vue 3 + Vite)
│   ├── backend-mock/   # 后端 Mock 服务 (Nitro)
│   ├── server/         # Node.js 服务端
│   └── child-vue3-vite/# 子应用示例
├── docs/               # 项目文档 (VitePress)
├── internal/           # 内部工具库与配置
│   ├── lint-configs/   # ESLint & Prettier 配置
│   └── node-utils/     # Node 工具函数
└── package.json
```

## 🛠️ 环境要求

- **Node.js**: >= 20.10.0
- **pnpm**: >= 9.12.0

## 🚀 快速开始

### 1. 安装 pnpm

如果尚未安装 pnpm，请运行：

```bash
npm install -g pnpm
```

### 2. 安装依赖

在项目根目录下运行：

```bash
pnpm install
```

### 3. 启动项目

启动主应用：

```bash
pnpm dev
# 或针对特定应用启动
pnpm dev:web    # 启动 web 应用
pnpm dev:docs   # 启动文档站
```

### 4. 构建项目

```bash
pnpm build
```

## 📜 常用脚本

| 脚本          | 说明                              |
| ------------- | --------------------------------- |
| `pnpm dev`    | 启动所有应用 (Turbo)              |
| `pnpm build`  | 构建所有应用                      |
| `pnpm lint`   | 代码风格检查                      |
| `pnpm format` | 代码格式化                        |
| `pnpm check`  | 执行所有检查 (类型、拼写、依赖等) |
| `pnpm test`   | 运行单元测试                      |

## 👤 测试账号

| 角色        | 用户名 | 密码   |
| ----------- | ------ | ------ |
| Super Admin | stall  | 123456 |
| Admin       | admin  | 123456 |
| User        | jack   | 123456 |

## 📄 License

MIT
